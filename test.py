import telebot
import config
import json
# api_token = config.BotToken
# webhook_host = config.Domain
# webhook_url = f"{webhook_host}bot"
# bot = telebot.TeleBot(api_token, threaded=False)
# bot.remove_webhook()
# print(webhook_url)
# bot.set_webhook(url=webhook_url)